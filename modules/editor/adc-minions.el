;;; adc-minions.el --- Aneesh's Emacs Config: minions configuration.

;;; Commentary:

;; Basic configuration for minions.

;;; Code:

(use-package minions
  :config
  (minions-mode 1))

(provide 'adc-minions)
;;; adc-minions.el ends here.
