;;; adc-whichkey.el --- Aneesh's Emacs Config: which-key configuration.

;;; Commentary:

;; Basic configuration for which-key

;;; Code:
(use-package which-key
  :config
  (which-key-mode 1))

(provide 'adc-whichkey)
;;; adc-whichkey.el ends here
